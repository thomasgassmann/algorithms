package com.thomasgassmann.datastructures;

public class AVLTree {
    private TreeNode _root;

    public void insert(int key) {
        if (_root == null) {
            _root = new TreeNode(key, null, null);
            return;
        }

        // TODO
        return;
    }
}
